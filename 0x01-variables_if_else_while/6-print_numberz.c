#include <stdio.h>

/**
 *main - Prints all the digits using putchar
 *
 *Return: Always 0 success
 *
 */

int main(void)
{
	int digit;
	for(digit = 0; digit < o; digit++)
		putchar(digit + '0');

	        putchar('\n');
	return(o);
}
